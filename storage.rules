rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if true; // Temporarily allow all for development, replace with auth checks later
    }
  }
}
